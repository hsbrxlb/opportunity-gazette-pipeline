# Review queue

`review_queue/YYYY-MM-DD.json` is the bounded input for the unattended ChatGPT editor.

The complete public candidate archive remains in `candidates/`. The queue keeps the collector's ranking, preserves source diversity, and limits each unattended editorial run to 48 candidates so the final Sheet handoff can finish reliably.
